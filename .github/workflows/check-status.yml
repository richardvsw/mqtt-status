name: Check MQTT broker status

on:
  # 2026-08-22: the built-in schedule trigger was removed -- an external
  # cron job already calls this workflow's workflow_dispatch API on its
  # own schedule. Keeping both meant every run fired twice (the
  # workflow's own cron AND the external one), doubling MQTT connection
  # attempts and Cloudflare API calls for zero benefit. workflow_dispatch
  # alone is enough: the external cron is what actually drives this now.
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: pip install -q paho-mqtt
      # MQTT_CHECK_USER/PASS: repo Settings -> Secrets and variables ->
      # Actions -- added 2026-08-22 so the checker can do a real
      # authenticated CONNECT instead of a plain TCP check (see
      # check_and_render.py's own comments for why that matters). Never
      # committed in plaintext.
      #
      # 2026-09-09: this workflow is now the ONLY publisher, for both
      # broker and bot status -- the LXC-side timer (deploy/systemd/
      # run_and_push.sh, mqtt-status-lxc.timer) that used to run this
      # locally every 2 min (for lower latency numbers and a genuine
      # homelab-offline fallback) has been retired, since the marginal
      # value (2-min vs this workflow's own ~10-min cadence, plus one
      # extra per-row latency figure) wasn't worth the added complexity
      # of two independent publishers and the "did the LXC already
      # publish" guard that entailed. This means there's no longer a
      # "skip if already published elsewhere" case to check for -- both
      # steps below always run.
      - run: python3 check_and_render.py
        env:
          MQTT_CHECK_USER: ${{ secrets.MQTT_CHECK_USER }}
          MQTT_CHECK_PASS: ${{ secrets.MQTT_CHECK_PASS }}
      # 2026-09-09: dropped the update_dns.py / mqtt.rivi.my.id
      # Cloudflare DNS failover step that used to run here -- retired
      # once meshnode.id's own mqtt.meshnode.id alias (fronted by its
      # own load balancer) was confirmed to already provide the same
      # failover, making the extra DNS layer redundant.
      # 2026-09-04: check_bot_status.py now polls the bot's own public
      # API (meshbot.rivi.my.id) directly over HTTPS -- reachable from
      # here just like anywhere else on the internet, so this gets a
      # real live check of mesh_bot/meshtasticd, not an inferred one.
      - run: python3 check_bot_status.py
      - name: Commit broker status if changed
        # 2026-08-22: GitHub-hosted runners default to UTC -- TZ here
        # makes the commit's own date/message consistently WIB.
        env:
          TZ: Asia/Jakarta
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html history state.json log.jsonl brokers.json notice.json
          if git diff --cached --quiet; then
            echo "no broker changes to publish"
            exit 0
          fi
          git commit -m "Update broker status $(date +%Y-%m-%dT%H:%M:%S%z) WIB"
          # 2026-09-09: retries here now cover the ONLY remaining source
          # of a push race -- this same workflow's own bot-status commit
          # step running right after this one in the same job, both
          # touching the same branch. `-X ours` keeps this step's
          # freshly-computed data on conflict.
          for i in 1 2 3; do
            if git push; then
              exit 0
            fi
            echo "push rejected (attempt $i), rebasing onto origin/main and retrying"
            git fetch origin main
            git rebase -X ours origin/main
          done
          echo "failed to push broker status after 3 attempts"
          exit 1
      - name: Commit bot status if changed
        env:
          TZ: Asia/Jakarta
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add bot-status.html bot_history bot_state.json bot_log.jsonl
          if git diff --cached --quiet; then
            echo "no bot status changes to publish"
            exit 0
          fi
          git commit -m "Update bot status $(date +%Y-%m-%dT%H:%M:%S%z) WIB"
          for i in 1 2 3; do
            if git push; then
              exit 0
            fi
            echo "push rejected (attempt $i), rebasing onto origin/main and retrying"
            git fetch origin main
            git rebase -X ours origin/main
          done
          echo "failed to push bot status after 3 attempts"
          exit 1
